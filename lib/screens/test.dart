import 'package:flutter/material.dart';

class Testing extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Kaar e kamal'),
      ),
      body: 
      Container(
        child: Center(child: Text('Testing Screen'),),),
    );
  }
}